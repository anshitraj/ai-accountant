kind = "web"
previewPath = "/"
title = "FinVerify OS"
version = "1.0.0"
id = "artifacts/finverify-os"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 21950

[services.development]
run = "pnpm --filter @workspace/finverify-os run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/finverify-os", "run", "build" ]
publicDir = "artifacts/finverify-os/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "21950"
BASE_PATH = "/"
